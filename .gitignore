# Build Jekyll
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Dépendances locales (le lock n'est pas versionné pour ne pas figer le build Netlify)
Gemfile.lock
vendor/
.bundle/

# Système
.DS_Store
